import { withStyles } from '@material-ui/core/styles';
import NotFound from '../../components/NotFound/NotFound';
import styles from '../../components/NotFound/NotFound.styles';

export default withStyles(styles)(NotFound);